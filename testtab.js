//Tableau bite
let bite=["a","b","c","d","e"];

function bjr(bitef)
{
   for (let i = 0; i < bitef.length; i++) {
    console.log(bitef[i]);
    
   }
   return bitef;
}

bjr(bite);


function fablegros(salope,grossepute)
{
    result=salope+grossepute;
    console.log("t'es toujours gros pd "+ result);
    return result;
}
